Bootstrap: docker
From: python:3.12.4-slim-bookworm

%files
    env.txt

%post
    # OS dependencies
    apt update
    apt install -y procps

    # lingualabpy installation
    . /env.txt
    pip install --root-user-action=ignore ${APP_NAME}==${APP_VERSION}

    # cleaning
    rm env.txt