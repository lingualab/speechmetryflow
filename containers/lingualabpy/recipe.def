Bootstrap: docker
From: python:3.12.4-slim-bookworm

%post
    export PIP_ROOT_USER_ACTION=ignore
    export PIP_DISABLE_PIP_VERSION_CHECK=1

    # OS dependencies
    apt update
    apt install -y procps ffmpeg

    # lingualabpy installation
    pip install {{APP_NAME}}=={{APP_VERSION}}
